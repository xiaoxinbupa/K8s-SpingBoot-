mybatis:
  mapper-locations: classpath:mapper/*.xml
logging:
  level:
    com.tarena.passport: trace
spring:
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
  profiles:
    active: dev

knife4j:
  enable: true
server:
  port: 8099

